# Garagely 产品审计与开发方案(v2 功能补全)

创建:2026-06-03 · 依据:第二轮深度调研(竞品核心功能矩阵 + 用户痛点/使用习惯 + 2026 table-stakes,三份独立调研收敛)
原则:**纯离线 / 无账号 / 买断 不动摇**;一次性产品级落地,不分阶段、不降级。

---

## 〇、现状审计

### A. 已经做对的(护城河,继续守,别重造)
调研里竞品被骂最惨的几条,我们恰好已避开:
- ✅ 自由补录历史 + 任意编辑 + 改里程不倒退(Fuelly/CARFAX 头号 1 星雷)
- ✅ 加油 full/partial 标记,MPG 只在满箱之间算(竞品 "300 MPG" bug 我们没有)
- ✅ 纯本地 / 无账号 / 买断 / 不丢数据 —— 调研结论:**这本身是 2026 最大卖点**(用户在逃离会丢数据、强制登录的老牌)

### B. PRD §4 已交付(SelfTest 12/12)
多车 CRUD、记录(类型/日期/里程/费用/备注/1图)、加油+油耗、日期+里程提醒、模板、统计、CSV/PDF 导出、Widget、App Intents/Siri、iCloud(Pro)、Dynamic Type、VoiceOver、6 图标、买断+恢复。

### C. 本次审计发现的核心缺口(=本方案要补的)
1. 🚩 **提醒是"死的"**:记了保养不会完成/顺延对应提醒;里程提醒发不了推送 —— 三份调研一致排第一
2. 支出只有"油+保养",**TCO 残缺**(无保险/年检/牌照/洗车/过路/月供…)
3. 每记录**仅 1 张图**,无文档手套箱(保险卡/行驶证/保修单)
4. **无 EV/充电**支持(只会算加仑)
5. 无**车辆 Specs 卡**(配件柜台查机滤号/胎压等"读"场景)
6. 零碎:单箱里程/最好最差油耗、卖车归档+买家历史、多币种、保修到期

---

## 一、本次新增范围(6 大块,全部纯离线)

| # | 模块 | 免费/Pro | 工作量 | SwiftData 影响 |
|---|---|---|---|---|
| 1 | 提醒闭环 + 里程→日期预测 + 提前量 | **免费(核心承诺)** | M | Reminder 加字段 + 派生 mileage model |
| 2 | 支出/TCO(独立 Expense 概念) | 记录免费;高级 TCO 分析 Pro | S–M | 新增 Expense 模型 |
| 3 | 多图 + 文档手套箱 + 到期提醒 | 免费 | S–M | 新增 Attachment 模型 |
| 4 | EV/充电(kWh/电耗/家充公充/PHEV/电池记录) | 免费 | M | FuelEntry 扩展 + 新增 EVReading |
| 5 | 车辆 Specs 卡 | 免费 | S | Vehicle 加 specPairs |
| 6 | 单箱里程·最好最差·归档卖车·多币种·保修 | 导出/分析 Pro;其余免费 | S each | Vehicle/FuelEntry 加字段 |

> 门控原则:新功能**多数免费**(强化"反巨头"的免费核心,驱动口碑);Pro 仍是「不限车 + 高级 TCO 分析 + 导出 + iCloud + 模板 + 图标」。付费墙 6 条权益保持全有真门控。

---

## 二、逐功能详细方案

### 模块 1 —— 提醒闭环(旗舰,第一优先)

**核心洞察**:iOS 只能按"日期"排通知,所以里程提醒必须先换算成预计日期才能真推送。

**数据模型(Reminder 扩展)**
```
Reminder 增加:
  leadMiles: Int = 200          // 提前量(里程)
  leadDays: Int = 14            // 提前量(天)
  anchorMileage: Int?           // 当前周期的锚点(上次完成时的里程)
  anchorDate: Date?             // 当前周期的锚点日期
  linkedKindRaw: String?        // 哪种保养类型可"完成"它(默认=kind)
  snoozedUntil: Date?
  lastFiredCycle: String?       // 去重:一个周期只通知一次
```

**派生里程速率(不存储,实时算)** —— `VehicleMileage`
- 取该车所有带里程的时间点(保养 + 加油),近 6–12 个月窗口
- `milesPerDay = (maxOdo - minOdo) / 跨越天数`
- 守卫:剔除里程倒退/异常跳变;数据点 <2 或跨度 <14 天时回退默认 **30 mi/day**(置信度 low)

**里程到期→预计日期**
```
milesRemaining = dueMileage - lastOdo
predictedDate  = lastOdoDate + (milesRemaining / milesPerDay) 天
fireDate = min( 日期路径: dueDate - leadDays,
                里程路径: predictedDate(dueMileage - leadMiles) )
排一条 UNCalendarNotificationTrigger 在 fireDate
```
**每次开 app / 每条新里程进来都重算重排** → 纯本地却像"盯着里程表"。

**完成闭环(交互)**
- 存保养记录时,若该车有 `linkedKind == 记录类型` 的活跃提醒 → 保存确认条弹:
  > ✅ 已记录 换油 @47,300 mi ·「换油」提醒 due 47,500 mi
  > [标记完成并安排下次] [不是这条]
- 确认后:`anchor = (记录里程, 记录日期)` → 重算 `dueMileage/dueDate` → 新 cycle → 重排通知 → Toast「下次换油 52,300 mi(约 2026-12)」
- 通知动作:**立即记录**(深链预填表单→再走闭环)/ **稍后提醒**(snooze)/ **跳过本次**(从今天里程顺延,不建记录)
- 提醒列表每行实时倒计时「约 210 mi / 18 天」,三色;同内容喂 Lock-Screen widget

**迁移**:老提醒保留现有 due,默认 lead 200mi/14天,首次进入提示一次「设为每 ___ mi/月 重复」加入闭环,不偷偷造周期。

**验收**:记录匹配保养→提醒顺延正确;里程提醒在无网下排出真通知;开 app 重算。

---

### 模块 2 —— 支出 / 总持有成本(TCO)

**模型:独立 Expense(不污染保养记录)**
```
@Model Expense
  id, vehicle?
  categoryRaw: String           // insurance/registration/tax/wash/tolls/parking/loan/fine/fee/accessory/other
  amount: Double
  currencyCode: String = "USD"  // 配合多币种
  date: Date
  mileage: Int?                 // 可选,有则也喂里程模型
  note: String
  recurrenceRaw: String?        // none/monthly/yearly(月供/保险→可生成日期提醒)
  // 附件复用模块 3 的 Attachment
```
- 录入免费;**recurring 支出(保险/月供/年检)可生成日期提醒**,复用模块 1 引擎 →「保险续费还有 30 天」
- 统计升级为真 TCO:`油 + 充电 + 保养 + 支出` 汇总;每英里成本;按类目(油/养/险/税/其他)拆;**固定 vs 变动**拆分;单车 + 全车队
- 导出 CSV/PDF 加 `recordType` 列(fuel/charge/service/expense)→ 完整财务台账
- **门控**:支出录入 + 基础合计免费;每英里成本 / 固定变动拆分 / 类目深析维持 Pro(与现有 analytics 一致)

**验收**:支出进总额与每英里成本;recurring 生成提醒;导出含 recordType。

---

### 模块 3 —— 多图 + 文档手套箱

**模型:统一 Attachment(CloudKit 友好,每附件一个 owner)**
```
@Model Attachment
  id, kindRaw: String           // photo / document
  filename: String
  data: Data                    // 图片(jpegForStorage 压)或 PDF/文件
  createdAt: Date
  // 互斥 owner(其一)
  vehicle?: Vehicle             // 手套箱:保险卡/行驶证/保修单
  serviceRecord?: ServiceRecord
  fuelEntry?: FuelEntry
  expense?: Expense
```
- 记录/加油/支出:**多张照片**(替换现 1 张 photoData;迁移:老 photoData → 建一条 Attachment)
- 车辆「手套箱」区:存保险卡/行驶证/保修/PDF,可加**到期日期** → 复用模块 1 日期提醒(到期前提醒)
- 多页收据支持(VMT 砍成单页烧了用户)
- **门控**:免费(电子手套箱是核心利益,且是对 AUTOsist 的对标)

**验收**:一条记录多图;车辆文档+到期提醒;CloudKit 不超记录尺寸(用 Attachment 关系,非内联数组)。

---

### 模块 4 —— EV / 充电(PHEV 油电同车)

**FuelEntry 扩展为"加注/充电"**
```
FuelEntry 增加:
  energyKindRaw: String = "gas" // gas / electric
  // electric 时 volume 复用为 kWh,cost 为充电费
  chargeLocationRaw: String?    // home / public
经济性:gas→MPG 或 km/L;electric→mi/kWh 或 kWh/100mi(按 energyKind 分别算)
PHEV:同一车可同时有 gas 与 electric 记录,经济性分轨展示
```
**Vehicle 加 powertrainRaw**:gas / diesel / ev / phev → 驱动:
- 加注表单默认形态(EV 默认充电)
- 模板换 EV 适配(轮胎/座舱滤/刹车油/冷却液/12V 电瓶/胎耗,**无换油**)
- Specs/统计单位

**EV 读数(电池健康/续航)** —— 轻量
```
@Model EVReading { id, date, mileage, estRangeMiles?, sohPercent?, vehicle? }
图表:续航/SoH 随时间(冬季掉电焦虑 + 卖车前看衰减)
```
- TCO 把充电费并入「每英里成本」(PHEV 油+电合算)→ 电车主想"证明省钱"的核心
- **门控**:免费

**验收**:充电记录算 mi/kWh;PHEV 双轨;EV 模板不含换油;充电费进 TCO。

---

### 模块 5 —— 车辆 Specs 卡(配件柜台速查)

**Vehicle 加** `specPairs: [SpecPair]`(Codable {label,value} 数组,小,CloudKit 友好)
- 预置常用项可空填:机滤号、空滤号、座舱滤、雨刷尺寸、大灯/雾灯灯泡号、轮胎规格、胎压(前/后)、电瓶组号、机油规格容量、扭矩
- 详情页常驻「Specs」卡,**强调读**(很多次打开是查不是写);可编辑/增删自定义行
- **门控**:免费(高价值低成本,AUTOsist 五星点)

**验收**:Specs 卡常驻可读;自定义行增删;随车 iCloud 同步。

---

### 模块 6 —— 零碎补全

| 子项 | 说明 | 门控 |
|---|---|---|
| 单箱里程 / 最好·最差·平均油耗 | 现有满箱逻辑上加排行,进 Fuel/Stats | 免费 |
| 卖车**归档**(非删除)+ 买家历史 PDF | Vehicle 加 `isArchived`;归档车不占车库但数据留;一键「Vehicle History Report」PDF 交接(=情感高潮+口碑) | 归档免费;PDF 走 Pro 导出 |
| 多币种 | 每记录/支出 `currencyCode`;按各自币种格式化;统计按主币种聚合(不做汇率联网,标注混币) | 免费 |
| 保修到期 | 用 Reminder(kind .custom 标题 Warranty)+ 日期/里程 due,复用引擎;入口在手套箱 | 免费 |

---

## 三、故意不做(联网,违背离线定位 / 巨头地盘)
VIN·车牌解码、NHTSA 召回查询、油价·加油站地图、社区油耗对比、修理厂目录、修理报价、OEM 原厂保养表(数据授权)、GPS 行程追踪、OBD 蓝牙硬件。
→ 这些都要后端,且稀释"纯本地"故事;明确**跳过**。(若未来要,做成可选的、不破坏离线的增值,不进本期)

---

## 四、数据迁移与 CloudKit 兼容
- **新增模型**:Expense、Attachment、EVReading;**字段扩展**:Reminder(lead/anchor/linked/snooze)、FuelEntry(energyKind/location)、Vehicle(powertrain/specPairs/isArchived/currency)
- 全部满足 CloudKit:每属性有默认值、关系可选、无 `.unique`;大二进制走 Attachment 关系(非内联数组),避免超 CKRecord 尺寸
- 迁移脚本:老 `ServiceRecord.photoData`/`FuelEntry.photoData` → 各建一条 Attachment 后置空(或保留兼容读取);ContainerFactory schema 加新模型;SelfTest 容器同步加
- 轻量迁移(加字段+加模型)SwiftData 自动 lightweight migration 可过;首启做一次 photoData→Attachment 回填

---

## 五、落地顺序(一次性交付,内部构建序,非产品分阶段)
1. 模型层:扩 Reminder/FuelEntry/Vehicle + 新增 Expense/Attachment/EVReading + 迁移 + ContainerFactory/SelfTest
2. **模块 1 提醒闭环**(引擎 + 完成流 + 通知重排 + 列表倒计时 + 迁移提示)
3. **模块 2 支出/TCO**(Expense CRUD + recurring 提醒 + Stats TCO + 导出列)
4. **模块 3 多图+手套箱**(Attachment + 多图 UI + 车辆文档区 + 到期提醒)
5. **模块 4 EV/充电**(powertrain + 充电表单 + mi/kWh + EV 模板 + EVReading 图表)
6. **模块 5 Specs 卡**
7. **模块 6 零碎**(单箱排行 + 归档/买家PDF + 多币种 + 保修)
8. 全量重测 + 截图审计 + 付费墙复核

---

## 六、测试与验收
- **SelfTest 扩展**(内存库):里程速率/预测日期、完成闭环顺延、recurring 支出→提醒、TCO 含支出+充电、Attachment 多图、mi/kWh、归档不丢数据、CSV recordType 列、多币种格式 —— 全 PASS
- **截图审计**:提醒列表倒计时、完成确认条、支出录入、TCO 统计、手套箱、多图、EV 充电表单、Specs 卡、归档/买家PDF、大字号不破版
- Build 绿;正常启动不崩;门控 free/Pro 各路径正确

---

## 七、上架影响
- **付费墙**:维持 6 条真门控;新免费功能强化「反巨头」叙事(数据永远是你的)
- **ASO**:新增可打关键词候选 —— `car expenses`/`total cost of ownership`/`EV charging log`/`vehicle documents`/`maintenance reminder`;描述加「reminders that actually fire」「your data, forever, on device」
- **隐私**:全离线不变;Attachment/文档仍本地;PrivacyView 增「documents stored locally」一句
- **个人账号安全**:仍 Utilities 纯本地、不碰支付处理/Health/Gov、无硬件/Web3 —— 不触红线

---

> 一句话:本期把 Garagely 从「能记账的工具」补成「真正盯着你车、算清总成本、装得下整个手套箱、油电通吃」的完整产品,且全程不联网、不要账号、买断不变。
