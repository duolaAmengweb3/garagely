# Garagely 上架操作手册

> 个人开发者账号 · Utilities 类 · 免费下载 + $5.99 一次买断
> 分工:**Claude 把代码和能本地/API 做的都干掉**;只剩"建 App 记录""建 CloudKit 容器""填 team""首个 IAP 网页勾选""Xcode Cloud 首次授权"这种**只能你手动**的,做完我接着用 API 跑完。

---

## 📍 当前进度(2026-06-03)
**代码已产品级完工并测过(SelfTest 21/21),但还没进 App Store Connect。** 比 Recurra 早一截:Recurra 当时 App 记录已建、元数据已灌;Garagely 现在是**代码完成、待开上架管线**。
诚实清单见下:已完成的是工程/设计/图标/文案;待做的是截图、Pages、Bundle ID 注册、建 App 记录、CloudKit 容器、提审。

---

## ✅ 已完成(Claude)

| 项 | 状态 |
|---|---|
| 产品代码(PRD §4 全功能 + v2 补全:提醒闭环/支出TCO/多图手套箱/EV充电/Specs/归档)| ✅ **SelfTest 21/21**,模拟器全屏审计 |
| 设计:Workshop Ledger(纸+石墨+安全橙,禁玻璃)| ✅ 真机验证全屏 + Dynamic Type 不破版 |
| App 图标(你给的 master 1024 + 5 备用图标 CoreImage 调色)| ✅ 已装进 `Assets.xcassets`,CFBundleAlternateIcons 已配 |
| 工程配置(1.0 / build 1 / App Group / IAP id / Privacy Manifest / Widget / App Intents)| ✅ |
| Per-config 权限(Debug=App Group;Release=+iCloud)| ✅ 模拟器可跑,iCloud 待签名期绑容器 |
| App 内隐私页(PrivacyView)+ mailto 支持 + StoreKit 买断/恢复 | ✅ |
| ASO 文案(名/副标题/关键词/描述/促销/What's New)| ✅ `上架文案.md` |

---

## 🛠 待 Claude 做(代码外,但我能本地/API 做 —— 给你建好 App 记录前后我就跑)

1. **上架截图 5–6 张**(6.9″ 1320×2868,痛点文案海报):车库总览 / 提醒倒计时 / 加保养+照片 / TCO 总成本 / EV 充电 / "$5.99 once · No account"。→ 我用模拟器出图 + 套海报文案。
2. **GitHub 仓库 + Pages**(隐私政策 + 支持页 → 拿到真实 Privacy/Support URL 供 ASC)。
3. **Bundle ID 注册**(ASC API 可做):`com.duolaameng.garagely` + `com.duolaameng.garagely.widget`。
4. 建好 App 记录后:灌 ASC 元数据 / 传截图 / 建 IAP→READY / 上架 175 区 / attach build / 提审 / 全网一致性 audit。

---

## ⏳ 只能你手动的 5 件事

### 1) 建 App 记录(必须,网页)
ASC 公开 API 不能建 App(POST /v1/apps 永远 403)。到 [App Store Connect](https://appstoreconnect.apple.com) → My Apps → ➕ New App:
- 平台 **iOS**
- 名称 **Car Maintenance: Garagely**(若被占用换备选 `Garagely: Car Maintenance`)
- 主语言 **English (U.S.)**
- Bundle ID 选 **com.duolaameng.garagely**(我会先用 API 注册好,下拉能选到)
- SKU **GARAGELY001** · 完全访问权限
> 建好把 **App ID(数字)** 给我,我立刻 API 灌:类目(Utilities/Lifestyle)、价格(免费)、隐私(No Data Collected)、描述/关键词/截图、上架 175 区。

### 2) 建 CloudKit 容器 + 给 App ID 开 iCloud 能力(Garagely 独有)
Pro 的 iCloud 同步要一个真容器。开发者后台 → Identifiers:
- iCloud Containers → ➕ 建 **`iCloud.com.duolaameng.garagely`**
- App ID `com.duolaameng.garagely` → Capabilities 勾 **iCloud (CloudKit)** → 关联上面的容器
> 这步做完,Release 包的 `Sources/Garagely.release.entitlements` 里的容器才生效;不做的话 App 仍能跑(自动降级本地),只是 Pro 的"iCloud 同步"不工作。

### 3) 填开发团队 ID(出 Release 包)
`project.yml` 里 `DEVELOPMENT_TEAM` 目前留空。把你的 **Team ID** 给我(或你在 Xcode 里选一次),Release archive 才能签名。

### 4) 首个 IAP 网页勾选一次(铁律)
我会用 API 把 `com.duolaameng.garagely.pro`($5.99 非消耗型)建好填齐(名称/价/截图/availability → READY_TO_SUBMIT),但**首个 IAP 必须你在网页版本页 "In-App Purchases" 区手动勾选一次**(API 无此接口)。
> ⚠️ 顺序铁律:**先让 IAP READY + 网页勾选,再提交版本**;绝不先单独提交版本(会锁死勾选区)。

### 5) Xcode Cloud 首次授权(出包)
首次连 GitHub 给 Xcode Cloud 需要你网页点一次授权(Session Proxy bootstrap)。授权后我配 **tag-only(`v*`)** 工作流。若首发卡 auth,退路:Xcode Cloud archive 出 IPA → `xcrun altool` 上传绕过。

---

## 🔜 你做完上面后,Claude 接着跑(API)
1. App 信息:类目 Utilities(主)/ Lifestyle(次)、年龄 4+、隐私 No Data Collected
2. 1.0 元数据:描述/关键词/副标题/促销文本/What's New/支持&隐私 URL/版权
3. 上传 5–6 张截图(6.9″)
4. 建 IAP `com.duolaameng.garagely.pro` + 价 + 审核截图 + availability → READY(等你网页勾选)
5. App 上架 175 区(`POST /v2/appAvailabilities`)
6. Xcode Cloud 出 build → attach → 提审(含 IAP)
7. 提审前跑**全网一致性 audit**(binary / ASC / 公开页 / in-app 七表面同一个故事)

---

## ⚠️ 个人账号安全自检(提交前过一遍)
- ✅ Utilities 纯本地记录,不碰支付处理 / Health / Finance / Gov / 车机硬件 / Web3
- ✅ 相机/相册仅用于收据照片,Info.plist 文案已写清
- ✅ 召回查询、VIN 解码、OBD 等联网/硬件功能**明确不做**(避边界)
- ✅ iCloud 走用户私有 CloudKit,不收集数据 → 隐私仍 No Data Collected

---

## 关键信息速查
- Bundle:`com.duolaameng.garagely` · Widget:`com.duolaameng.garagely.widget` · IAP:`com.duolaameng.garagely.pro`
- CloudKit 容器:`iCloud.com.duolaameng.garagely` · App Group:`group.com.duolaameng.garagely`
- 版本 1.0 / build 1
- 审核联系人:Huan Xu / +86 13777586836 / xxhhuan2022@163.com
- 文案:`上架文案.md` · 方案:`产品审计与开发方案.md` · PRD:`产品需求.md`
- 工程:xcodegen(`project.yml`) · 测试:`SIMCTL_CHILD_SELFTEST=1` 跑内存库审计
